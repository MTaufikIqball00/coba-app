import { Metadata } from 'next';
import React from 'react'

export const metadata: Metadata = {
  title: {
    absolute: "Blog Page",
  },
};

const blog = () => {
  return (
    <div>blog</div>
  )
}

export default blog